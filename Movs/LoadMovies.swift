//
//  LoadMovies.swift
//  Movs
//
//  Created by Lorhany Moraes on 11/07/22.
//

import Foundation
/*
class LoadMovies: MoviesTableViewController {

var loadMovies: [MoviesResult] = []
var loadingMovies = false
var currentPage = 0
var total = 0

func loadPage() {
    loadingMovies = true
    MovieAPI.loadPage(page: currentPage) { (info) in
        if let info = info {
            self.loadMovies += info.results
            self.total = info.totalResults ?? 0
            
            DispatchQueue.main.async {
                self.loadingMovies = false
                self.tableView.reloadData()
            }
        }
    }
}

}
*/
